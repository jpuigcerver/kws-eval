language: cpp

os:
  - linux

dist: trusty

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - libgtest-dev
      - google-mock

script:
  - mkdir build && cd build
  - CXX=$COMPILER ccmake ..
  - make
  - make test